// Importing the custom module
// const value = require('./math')
const {myAdd, sub} = require('./math'); //../, ./, ./math.js, ../foldername/math.js

// console.log(myAdd(2,3));
// console.log(value.sub(2,3));

const anything = require('./math')
anything()
